// OFF BY v4 runtime configuration.
// This file is intentionally safe to serve publicly.
// The Edge Function URL is not a secret. Never put a Supabase service-role key here.
window.OFFBY_CONFIG = Object.freeze({
  leaderboardEnabled: false,
  apiUrl: ""
});
